generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id          Int     @default(autoincrement())
  title       String  @unique
  price       Int
  category    String
  description String
  image       String?
}
